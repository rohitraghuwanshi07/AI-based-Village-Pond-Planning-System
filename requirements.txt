# --- Core web framework ---
fastapi==0.115.0
uvicorn[standard]==0.30.6

# --- HTTP client for calling free external APIs (Open-Meteo, Overpass, etc.) ---
httpx==0.27.2

# --- File upload support (required for contour map upload endpoint) ---
python-multipart==0.0.9

# --- Config / env vars ---
python-dotenv==1.0.1
pydantic-settings==2.5.2

# --- Database (PostgreSQL + PostGIS) ---
sqlalchemy==2.0.35
psycopg2-binary==2.9.9
geoalchemy2==0.15.2

# --- Geospatial / terrain processing ---
rasterio>=1.3.11
numpy>=2.0
scikit-image==0.24.0
shapely==2.0.6
pyproj==3.6.1
scipy>=1.13.0

# --- Image processing (as suggested in the assignment) ---
opencv-python-headless==4.10.0.84

# --- Testing ---
pytest==8.3.3
